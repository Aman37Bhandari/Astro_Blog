import type { NextConfig } from "next";

const nextConfig: NextConfig = {

  images: {
    domains: [
      'images.unsplash.com',
      'i.pravatar.cc',
      'i.pinimg.com',
      'api.dicebear.com',
      'placehold.co'
    ],
  },
};

export default nextConfig;